
class BoxWave extends GameObject{

    progress:number = 0;

    constructor() {
        super();
    }
    
    update() {
        // this.progress++;

        // if( this.progress > 30 ){
        //     this.progress = 0;
        //     let sizeW = BOX_SIZE_PER_WIDTH * Util.width;
        //     let sizeH = sizeW * Box.sizeRateH;
        //     new Box( sizeW * Util.randomInt(1, BOXES_IN_WIDTH-1), sizeH * Util.randomInt(2, 4), Util.randomInt(1,Box.maxHp) );
        // }
    }
}

